'use strict';

/**
 * favourite-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::favourite-list.favourite-list');
